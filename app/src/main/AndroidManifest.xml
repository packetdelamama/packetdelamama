<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.packetdelamama">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".CurseDisponibile"></activity>
        <activity android:name=".CereriDisponibile_DriverPerspective" />
        <activity android:name=".TransportaColet" />
        <activity android:name=".TrimiteSiPrimeste" />
        <activity android:name=".ActionPage" />
        <activity android:name=".Register_MainPage" />
        <activity android:name=".Login_MainPage" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>